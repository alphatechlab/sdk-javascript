const { setConfig, getConfig } = require('./src/config');
const { upload } = require('./src/upload');
const { getPdf } = require('./src/get-pdf');

module.exports = {
  setConfig,
  getConfig,
  upload,
  getPdf,
};
